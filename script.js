var typed = new Typed(".text", {
    strings: ["Frontend Developer", "Backend Developer", "Web Developer"], // Corrected strings and syntax
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true // Corrected loop syntax
});